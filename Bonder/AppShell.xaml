<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Bonder.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Bonder.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False">

    <!-- Onboarding Flow -->
    <ShellContent
        Title="SignIn"
        ContentTemplate="{DataTemplate views:SignInPage}"
        Route="SignIn" />

    <ShellContent
        Title="GenreSelection"
        ContentTemplate="{DataTemplate views:GenreSelectionPage}"
        Route="GenreSelection" />

    <ShellContent
        Title="TopBooksSelection"
        ContentTemplate="{DataTemplate views:TopBooksSelectionPage}"
        Route="TopBooksSelection" />

    <!-- Main App Tabs -->
    <TabBar Route="Main">
        <ShellContent
            Title="Discover"
            ContentTemplate="{DataTemplate views:SwipeDiscoveryPage}"
            Route="SwipeDiscovery"
            Icon="🔍" />

        <ShellContent
            Title="Library"
            ContentTemplate="{DataTemplate views:LibraryPage}"
            Route="Library"
            Icon="📚" />

        <ShellContent
            Title="Search"
            ContentTemplate="{DataTemplate views:SearchPage}"
            Route="Search"
            Icon="🔎" />

        <ShellContent
            Title="Profile"
            ContentTemplate="{DataTemplate views:ProfilePage}"
            Route="Profile"
            Icon="👤" />
    </TabBar>

    <!-- Detail Pages -->
    <ShellContent
        Title="BookDetails"
        ContentTemplate="{DataTemplate views:BookDetailsPage}"
        Route="BookDetails" />

</Shell>